{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal desktop permissions for ChoiceLens",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
